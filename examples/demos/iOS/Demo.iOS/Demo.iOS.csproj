<Project Sdk="Microsoft.NET.Sdk">
  <!-- iOS app (net10.0-ios; requires the ios workload + macOS host to build). Named {Solution}.iOS so the core SDK detects the project type. -->
  <Sdk Name="ktsu.Sdk" />
  <Sdk Name="ktsu.Sdk.iOS" />
  <ItemGroup>
    <PackageReference Include="Polyfill" PrivateAssets="all" />
  </ItemGroup>
</Project>
