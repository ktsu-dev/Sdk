namespace Alpha;

/// <summary>A trivial type, present so the project produces an assembly.</summary>
public static class Widget
{
	/// <summary>Returns the widget's name.</summary>
	public static string Name => "widget";
}
