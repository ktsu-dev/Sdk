<Project Sdk="Microsoft.NET.Sdk">
  <!-- macOS desktop app (net10.0, Exe, osx RIDs). Named {Solution}.macOS so the core SDK detects the project type. -->
  <Sdk Name="ktsu.Sdk" />
  <Sdk Name="ktsu.Sdk.macOS" />
  <ItemGroup>
    <PackageReference Include="Polyfill" PrivateAssets="all" />
  </ItemGroup>
</Project>
