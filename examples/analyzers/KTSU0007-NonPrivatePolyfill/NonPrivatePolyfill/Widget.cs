namespace NonPrivatePolyfill;

/// <summary>Compilable content so the project produces a compilation for the analyzer to inspect.</summary>
public static class Widget
{
    /// <summary>A trivial member.</summary>
    /// <returns>The answer.</returns>
    public static int Answer() => 42;
}
