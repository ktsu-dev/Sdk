<Project>
  <!--
    Common NuGet package properties
    Requires: Authors, Version, Description, Tags, ProjectUrl, etc. from metadata files
  -->
  <PropertyGroup>
    <!-- Assembly naming -->
    <AssemblyName Condition="'$(AssemblyName)' == ''">ktsu.$(MSBuildProjectName.Replace(" ", ""))</AssemblyName>
    <OutputType Condition="'$(OutputType)' == ''">Library</OutputType>

    <!-- Package identity -->
    <Title Condition="'$(Title)' == ''">$(AssemblyName)</Title>
    <Company Condition="'$(Company)' == ''">$(Authors)</Company>
    <Product Condition="'$(Product)' == ''">$(AssemblyName)</Product>
    <PackageId Condition="'$(PackageId)' == ''">$(AssemblyName)</PackageId>
    <PackageVersion Condition="'$(PackageVersion)' == ''">$(Version)</PackageVersion>

    <!-- Package metadata -->
    <PackageIcon Condition="'$(PackageIcon)' == ''">$(IconFileName)</PackageIcon>
    <PackageReadmeFile Condition="'$(PackageReadmeFile)' == ''">$(ReadmeFileName)</PackageReadmeFile>
    <PackageLicenseFile Condition="'$(PackageLicenseFile)' == ''">$(LicenseFileName)</PackageLicenseFile>
    <!--
      Release notes: an explicit file wins over the full solution changelog. CI (Invoke-DotNetPack in
      scripts/PSBuild.psm1) points PackageReleaseNotesFile at LATEST_CHANGELOG.md so a package carries
      only the notes for the version being released rather than the entire history. A relative path is
      resolved against the solution directory; an absolute path is used as given.
    -->
    <PackageReleaseNotesFilePath Condition="'$(PackageReleaseNotesFile)' != ''">$([MSBuild]::NormalizePath('$(SolutionDir)', '$(PackageReleaseNotesFile)'))</PackageReleaseNotesFilePath>
    <PackageReleaseNotes Condition="'$(PackageReleaseNotes)' == '' And '$(PackageReleaseNotesFilePath)' != '' And Exists('$(PackageReleaseNotesFilePath)')">$([System.IO.File]::ReadAllText('$(PackageReleaseNotesFilePath)').Trim())</PackageReleaseNotes>
    <PackageReleaseNotes Condition="'$(PackageReleaseNotes)' == ''">$(Changelog)</PackageReleaseNotes>
    <!--
      nuget.org rejects a push with HTTP 400 once ReleaseNotes passes 35000 characters, so cap the
      value at pack time instead of discovering the limit at publish time. The cap sits below the
      limit to leave room for the truncation marker.
    -->
    <PackageReleaseNotes Condition="'$(PackageReleaseNotes)' != '' And $(PackageReleaseNotes.Length) > 34000">$(PackageReleaseNotes.Substring(0, 34000))... (truncated due to NuGet length limits)</PackageReleaseNotes>
    <PackageDescription Condition="'$(PackageDescription)' == ''">$(Description)</PackageDescription>
    <PackageTags Condition="'$(PackageTags)' == ''">$(Tags)</PackageTags>
    <PackageProjectUrl Condition="'$(PackageProjectUrl)' == ''">$(ProjectUrl)</PackageProjectUrl>

    <!-- Package settings -->
    <IncludeSource Condition="'$(IncludeSource)' == ''">true</IncludeSource>
    <IncludeContentInPack Condition="'$(IncludeContentInPack)' == ''">true</IncludeContentInPack>
    <PackRelease Condition="'$(PackRelease)' == ''">true</PackRelease>
    <SymbolPackageFormat Condition="'$(SymbolPackageFormat)' == ''">snupkg</SymbolPackageFormat>
    <IsPackable Condition="'$(IsPackable)' == ''">true</IsPackable>

    <!-- Continuous Integration Build -->
    <ContinuousIntegrationBuild Condition="'$(ContinuousIntegrationBuild)' == ''">false</ContinuousIntegrationBuild>
    <ContinuousIntegrationBuild Condition="'$(GITHUB_ACTIONS)' == 'true' Or '$(TF_BUILD)' == 'true'">true</ContinuousIntegrationBuild>
  </PropertyGroup>
</Project>
